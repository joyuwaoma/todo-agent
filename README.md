# Todo Agent
A productivity AI agent built with Google ADK and Gemini.

## Setup
1. Clone the repo
2. Install dependencies: `pip install google-adk`
3. Add your API key to `.env`: `GOOGLE_API_KEY=your-key`
4. Run: `adk run agent.py`
